# 决策：原始回复附件尾部污染处理

- 决策时间：2026-09-21 16:01:30 +08:00
- 状态：已确认

## 观察

`task4_replies.json` 在合法 JSON 数组结束后出现三行非 JSON 文本，当前 SHA-256 为 `dedd12c52a2a1a86051f8cc8930780f2f9080d1192e028fa48806ed88941acd3`。`task4_ground_truth.json` 仍是合法 JSON。

## 决定

- 不修改、不提交原始招聘附件。
- 严格加载入口继续拒绝尾部垃圾，不能静默吞掉数据问题。
- 本轮实验只允许通过显式 `prepare-input` 步骤生成 `artifacts/staging/` 下的派生输入。
- 派生清单必须记录源文件哈希、派生文件哈希、解析到的记录数和尾部字节数。

## 为什么

直接修源文件会破坏现场；让正常加载器自动忽略尾部又会掩盖数据完整性问题。显式派生同时保留现场和可复现实验入口。
